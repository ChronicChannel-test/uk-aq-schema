# uk_aq_region_names

Lookup table for UK region codes to human-readable names.

## Fields
- region_code: Region code (primary key).
- region_name: Region name.
- created_at: Row creation timestamp (default now()).

## Notes
- Used for hex map region labels and metadata.
